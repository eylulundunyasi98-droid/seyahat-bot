{
  "name": "seyahat-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "cron": "node src/cron.ts"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}